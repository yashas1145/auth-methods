<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Express Authentication app</title>
        <link rel="stylesheet" href="styles/styles.css">
    </head>
    <body>
        <div class="flex-container">
            <form action="/noAuth">
                <button style="background-color: rgb(206, 89, 89);" type="submit">No authentication</button>
            </form>
            <form action="/basicAuth">
                <button style="background-color: rgb(108, 166, 224);" type="submit">Basic authentication</button>
            </form>
            <form action="/apiAuth">
                <button style="background-color: rgb(164, 194, 119);" type="submit">API key authentication</button>
            </form>
            <form action="tokenAuth">
                <button style="background-color: rgb(81, 131, 81);" type="submit">Token based authentication</button>
            </form>

            <div class="output">
                <% if(locals.data) { %>
                    <%= data %>
                <% }else { %>
                    Hit an API!
                <% } %>
            </div>
        </div>
    </body>
</html>